Package beacon is a simple implemention of UDP multicast beacon. It only supports IPv4 and only serves as a placeholder, since most multicast implementions in golang are currently broken under Windows.
